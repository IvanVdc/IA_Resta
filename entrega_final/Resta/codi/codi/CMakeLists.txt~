#PROJECT (Step1)

#FIND_PACKAGE(VTK REQUIRED)
#IF(NOT VTK_USE_RENDERING)
#  MESSAGE(FATAL_ERROR "Example ${PROJECT_NAME} requires VTK_USE_RENDERING.")
#ENDIF(NOT VTK_USE_RENDERING)
#INCLUDE(${VTK_USE_FILE})

#ADD_EXECUTABLE(dibuixa_resul dibuixa_resul.cxx)
#TARGET_LINK_LIBRARIES(dibuixa_resul vtkRendering)


cmake_minimum_required(VERSION 2.6)
PROJECT( NOMBRE_PROYECTO )
FIND_PACKAGE ( VTK )
IF ( VTK_FOUND )
INCLUDE( ${USE_VTK_FILE} )
ENDIF( VTK_FOUND )
FIND_PACKAGE(FLTK)
IF(FLTK_FOUND)
INCLUDE_DIRECTORIES(${FLTK_INCLUDE_DIR})
ENDIF(FLTK_FOUND)
SET(CMAKE_BUILD_TYPE "Debug")
INCLUDE_DIRECTORIES(
${CARPETA_CON_COD_FUENTE}
)
ADD_EXECUTABLE( dibuixa_resul dibuixa_resul.cxx)
TARGET_LINK_LIBRARIES ( dibuixa_resul vtkRendering
vtkRendering 
)
TARGET_LINK_LIBRARIES(dibuixa_resul ${FLTK_LIBRARIES})
TARGET_LINK_LIBRARIES(dibuixa_resul ${OPENGL_LIBRARIES})